# Checkpoint Semana 01 · PocketLog v0.1

```bash
kotlinc PocketLog.kt -include-runtime -d pocketlog-s1.jar
java -jar pocketlog-s1.jar
```

Conceptos: variables, tipos, `val`/`var`, funciones, `if/else`, String templates y null safety inicial. Sin colecciones ni POO.
