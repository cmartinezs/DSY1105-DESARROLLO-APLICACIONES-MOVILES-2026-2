# 01 · Colecciones y deuda de modelado

1. Crea y ejecuta `GestorActividades.kt`.

**Checkpoint:** aparece `Gestor iniciado`.

2. Trabaja con `MutableList<String>`, `for`, `forEach`, `filter`, `map` y `count` según contenido cubierto.

**Checkpoint:** puedes explicar recorrido vs transformación.

3. Representa temporalmente datos con listas paralelas.

**Checkpoint:** identifica al menos un riesgo de coherencia por índices.
