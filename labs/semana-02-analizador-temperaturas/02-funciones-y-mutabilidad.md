# 02 · Funciones y mutabilidad

1. Implementa `clasificarTemperatura(Int): String`.
2. Prueba 17, 18, 24 y 25.

**Checkpoint:** los límites clasifican correctamente.

3. Cuenta temperaturas >= 25 usando contador, `for` e `if`.

**Checkpoint:** puedes explicar el algoritmo sin `count`.

4. Crea una `MutableList` y agrega 26.

**Checkpoint:** la colección tiene ocho elementos.
